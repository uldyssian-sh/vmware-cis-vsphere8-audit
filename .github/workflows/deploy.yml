name: Deploy Development

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

permissions:
  contents: read
  security-events: write
  actions: read

  push:
    branches: [main]
  workflow_dispatch:

